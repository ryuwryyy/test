# OpenAI の API キー（画像生成）
OPENAI_API_KEY=sk-...
# 使う画像モデル（省略時 gpt-image-2）
OPENAI_IMAGE_MODEL=gpt-image-2
# Vercel Blob（Vercel で Blob ストアを接続すると自動で入る）
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...
# OGP の絶対 URL 用。本番ドメイン
NEXT_PUBLIC_SITE_URL=https://example.com
